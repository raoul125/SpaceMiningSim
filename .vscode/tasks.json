{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "CMake: build",
			"command": "${workspaceFolder}/build.sh",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "CMake build task"
		}
	]
}